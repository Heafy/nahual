"""
nahual.body

The "body" pipeline: MediaPipe Pose based detection, evaluated alongside the
hand-based "hands" pipeline (see BODY_PLAN.md). Currently detection only; no
heuristics, feature vectors or classifiers yet.

Modules:
    landmarker -- Canonical MediaPipe PoseLandmarker configuration.
"""
